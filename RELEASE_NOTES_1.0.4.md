# 记账本 Android v1.0.4

## 概述

本版本定位为**纯手动录入**的批发/个人记账客户端：在表单中逐项填写或粘贴内容，**不提供应用内语音解析入口**。搭配自建后端（MySQL + API）时，支持**账号密码登录与云端数据同步**。

## 主要功能

- **手动记账**：首页添加记录，支持默认字段（商品、数量、车牌号、金额）与自定义字段；可为字段设置「必填」。
- **云端登录**：在「设置」页使用用户名/密码登录；新用户可注册（需有效邮箱格式）。登录后账单写入服务器，多设备可同步。
- **本地模式**：未配置或未登录 API 时，数据保存在本机（IndexedDB）；可导出 JSON/CSV 备份与恢复。
- **统计与设置**：统计页查看汇总；设置页管理字段、备份与账号。

## 部署说明（自建后端）

1. 使用仓库根目录 `docker-compose.yml` 启动 MySQL 与 API（默认对外端口 **3001**）。
2. 首次启动后存在种子账号：**用户名 `admin`，密码 `123456`**（生产环境请尽快修改或新建账号）。
3. 打 APK 前在源码根目录配置 `.env` 中 **`VITE_API_URL`** 指向你的 API 地址，执行 `npm run build` 与 `npx cap sync android` 后再打包。

## 资源

- **APK**：见本 Release 附件 `kuaiji-v1.0.4.apk`（或项目官网下载页）。
- **源码**：`android/app/build.gradle` 中 `versionName` 为 **1.0.4**，`versionCode` 为 **5**。

## 与前一版本相比

- 强调**手动输入**工作流；已移除/关闭语音等智能录入相关界面入口（若需豆包等扩展，可参考仓库内 `DOUBAO_SETUP.md` 自行接入）。
