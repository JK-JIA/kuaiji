# 前端：指向 docker-compose 启用的 API（本机开发可填 http://localhost:3000）
# 生产请填 https 公网地址；打 Android 包时真机需能访问该地址（勿用 localhost）
VITE_API_URL=http://localhost:3000

# 豆包智能识别（可选）。复制本文件为 .env 后填写，不要将 .env 提交到 Git
# VITE_DOUBAO_API_KEY=
