# 前端：账本 API 根地址（无末尾斜杠）。本地开发可改为 http://localhost:3000
# 当前部署：Docker 映射宿主机 3000 端口；手机/外网需放通安全组与防火墙
VITE_API_URL=http://8.153.12.131:3000

# 豆包智能识别（可选）。复制本文件为 .env 后填写，不要将 .env 提交到 Git
# VITE_DOUBAO_API_KEY=
