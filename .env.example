# 前端：账本 API 根地址（无末尾斜杠）。本地直连后端可改为 http://localhost:3001
# Docker 默认对外端口 3001；手机/外网需放通安全组与防火墙
VITE_API_URL=http://8.153.12.131:3001

# 豆包智能识别（可选）。复制本文件为 .env 后填写，不要将 .env 提交到 Git
# VITE_DOUBAO_API_KEY=

# 默认不提供浏览器端界面；若需在浏览器里调试打包产物可设为 true（勿对外部署）
# VITE_ALLOW_BROWSER=true
