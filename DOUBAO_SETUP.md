# 豆包 AI 智能解析配置指南

本应用使用豆包大模型进行智能文本解析，支持自然语言输入。

**方舟 Responses API 官方说明**：[创建模型响应](https://www.volcengine.com/docs/82379/1569618)

## ✨ 功能特点

- 🎯 **自然语言输入**：不需要固定格式，随便说
- 🤖 **AI 智能识别**：自动提取商品、数量、车牌、金额等信息
- 📱 **配合输入法**：用微信、搜狗等输入法的语音输入，然后 AI 解析
- 💰 **超大免费额度**：新用户送 200 万 tokens，够用很久

## 💬 输入示例

**传统方式（需要固定格式）：**
```
苹果五斤川A12345
```

**现在可以这样说（自然语言）：**
```
今天卖了5斤苹果给川A12345的车，收了50块钱
```

```
西瓜10斤，车牌粤B88888
```

```
红薯100公斤3包，尾号1234，金额200元
```

AI 会自动识别并填入对应字段！

## 📝 获取豆包 API Key（5分钟）

### 1. 注册火山引擎账号
访问：https://www.volcengine.com/  
点击右上角"注册/登录"

### 2. 进入豆包控制台
访问：https://console.volcengine.com/ark/region:ark+cn-beijing/endpoint

### 3. 创建推理接入点
1. 点击"创建推理接入点"
2. 选择模型：**Doubao-lite-4k**（免费且快速）
3. 填写名称：记账本（或任意名称）
4. 点击"创建"

### 4. 获取信息
创建成功后，你会看到：
- **接入点 ID**（Endpoint ID）：例如 `ep-20241211185157-xqxfh`
- 点击"查看 API Key"获取 **API Key**

### 5. 配置到项目

在 **`kuaiji` 目录**下复制环境模板并填写 Key（`.env` 已被 git 忽略，勿提交密钥）：

```bash
# macOS / Linux
cp .env.example .env
# Windows（PowerShell / CMD）
copy .env.example .env
```

**正式环境（推荐）**：在 **ledger-api 服务端** `.env` / `docker-compose` 中配置（用户 App **无需**因换模型而重新打包）：

```
DOUBAO_API_KEY=你的方舟_API_Key
DOUBAO_MODEL=doubao-1-5-pro-256k-250115
# 可选
# DOUBAO_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3/responses
```

App 登录后调用 `POST /api/voice/parse`，由服务端请求火山方舟。修改后 **重启 api 容器** 即可生效。

App **不再**读取 `VITE_DOUBAO_API_KEY` / `VITE_DOUBAO_MODEL`；模型以服务端 `DOUBAO_MODEL` 为准。在 **设置 → 智能解析** 可查看当前服务端模型。

## 📦 重新构建 APK

配置完成后，重新构建：

```bash
# 1. 构建 Web 资源
npm run build

# 2. 同步到 Android 项目
npx cap sync android

# 3. 构建 APK
cd android
.\gradlew.bat assembleDebug
```

APK 位置：`android\app\build\outputs\apk\debug\app-debug.apk`

## 🎤 使用方法

### 方式 1：输入法语音输入（推荐）
1. 点击「记一笔」
2. 在语音区域的文本框中，**使用输入法的语音按钮**输入
3. 说话，例如："今天卖了5斤苹果给川A12345，收了50块"
4. 输入法会转成文字
5. 点击 **「智能填入」**（已配置 `VITE_API_URL` 且已登录时）或 **「填入首行」**（未连云端时）
6. 配置正确时，豆包会解析并填入下方字段与多行商品

### 方式 2：手动输入
1. 直接在语音区域文本框输入文字
2. 点击「智能填入」或「填入首行」（含义同上）

### 方式 3：传统方式
如果不想用 AI，也可以直接在下方字段逐个填写。

## 💰 费用说明

### 免费额度
- 新用户送 **200 万 tokens**
- 1 次解析约消耗 **200-500 tokens**
- 可以用 **4000-10000 次**
- 个人使用够用很久

### 超出后
- Doubao-lite 模型：¥0.0008/千 tokens
- 1 次解析约 **¥0.0002**（不到 1 分钱）
- 非常便宜

## ⚠️ 注意事项

### 网络要求
- 需要联网使用
- 建议在 WiFi 环境下使用

### 识别准确性
- 说话尽量清晰完整
- 包含关键信息：商品、数量、车牌等
- 如果识别不准，可以重新输入

### 隐私说明
- 文本会发送到豆包服务器进行解析
- 豆包会保留日志用于改进服务
- 如果担心隐私，可以直接填写下方字段

## ❓ 常见问题

**Q: 按钮一直是「填入首行」**  
A: 检查 App 是否配置 `VITE_API_URL` 并已登录；智能解析只走服务端 `/api/voice/parse`。

**Q: 模型报错或未开通**  
A: 在服务端设置 `DOUBAO_MODEL` 为火山方舟已开通的模型 ID，重启 ledger-api；App 内 **设置 → 智能解析** 可核对当前模型。

**Q: 提示"API Key 无效"**  
A: 检查 API Key 是否正确，是否有多余的空格

**Q: 提示"请求过于频繁"**  
A: 稍等几秒再试

**Q: 识别不准确**  
A: 尽量说完整的句子，包含所有关键信息

**Q: 不想用 AI 了**  
A: 可以直接在下方字段逐个填写，功能一样

## 🎉 总结

豆包 AI 智能解析：
- ✅ 支持自然语言输入
- ✅ 配合输入法语音输入使用
- ✅ 200 万 tokens 免费额度
- ✅ 识别准确率高
- ✅ 配置简单，5分钟搞定
- ⚠️ 需要网络连接
- ⚠️ 文本会上传到服务器

**让记账变得更简单、更自然！**
